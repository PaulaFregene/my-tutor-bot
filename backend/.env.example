# Backend environment template

# ============================================
# API Keys (Required)
# ============================================

# Groq API (for LLM - llama-3.3-70b-versatile)
GROQ_API_KEY=your_groq_api_key_here

# ============================================
# AWS S3 Configuration (Optional for Production)
# ============================================

# Enable S3 storage (set to "true" for production, "false" for local dev)
# When false, files are stored in ./uploaded_pdfs/ directory
USE_S3=false

# AWS Credentials (only needed if USE_S3=true)
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_S3_BUCKET=your-s3-bucket-name
AWS_REGION=us-east-1

# ============================================
# Optional Configuration
# ============================================

# FastAPI
PYTHONUNBUFFERED=1

# Frontend URL (for CORS in production)
FRONTEND_URL=https://your-app.railway.app

# Database (Railway provides PostgreSQL - only if migrating from SQLite)
# DATABASE_URL=postgresql://user:password@localhost:5432/tutorbot

# LlamaCloud (not currently used - using local RAG)
# LLAMA_CLOUD_API_KEY=your_api_key_here

# Chroma Cloud (not currently used - using local ChromaDB)
# CHROMA_API_KEY=your_chroma_api_key_here

# ============================================
# Usage Notes:
# ============================================
# 
# LOCAL DEVELOPMENT:
#   - Set USE_S3=false (or leave unset)
#   - Only GROQ_API_KEY is required
#   - Files stored locally in ./uploaded_pdfs/
#   - Vector DB stored locally in ./chroma_db/
#
# PRODUCTION DEPLOYMENT:
#   - Set USE_S3=true
#   - Configure all AWS S3 variables
#   - Set FRONTEND_URL for CORS
#
# S3 IAM PERMISSIONS NEEDED:
#   - s3:PutObject
#   - s3:GetObject
#   - s3:DeleteObject
#   - s3:ListBucket
